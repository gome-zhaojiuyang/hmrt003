<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.hmrtgroupchat.dao.HmrtGroupChatDao">
    
	<sql id="hmrtGroupChatColumns">
		a.id AS "id",
		a.groupid AS "groupid",
		a.userid AS "userid",
		a.articleid AS "articleid",
		a.content AS "content",
		a.create_dt AS "createDt"
	</sql>
	
	<sql id="hmrtGroupChatJoins">
	</sql>
    
	<select id="get" resultType="HmrtGroupChat">
		SELECT 
			<include refid="hmrtGroupChatColumns"/>
		FROM hmrt_group_chat a
		<include refid="hmrtGroupChatJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="HmrtGroupChat">
		SELECT 
			<include refid="hmrtGroupChatColumns"/>
		FROM hmrt_group_chat a
		<include refid="hmrtGroupChatJoins"/>
		<where>
			
			<if test="groupid != null and groupid != ''">
				AND a.groupid = #{groupid}
			</if>
			<if test="userid != null and userid != ''">
				AND a.userid = #{userid}
			</if>
			<if test="articleid != null and articleid != ''">
				AND a.articleid = #{articleid}
			</if>
			<if test="beginContent != null and endContent != null and beginContent != '' and endContent != ''">
				AND a.content BETWEEN #{beginContent} AND #{endContent}
			</if>
			<if test="beginCreateDt != null and endCreateDt != null and beginCreateDt != '' and endCreateDt != ''">
				AND a.create_dt BETWEEN #{beginCreateDt} AND #{endCreateDt}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="HmrtGroupChat">
		SELECT 
			<include refid="hmrtGroupChatColumns"/>
		FROM hmrt_group_chat a
		<include refid="hmrtGroupChatJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO hmrt_group_chat(
			id,
			groupid,
			userid,
			articleid,
			content,
			create_dt
		) VALUES (
			#{id},
			#{groupid},
			#{userid},
			#{articleid},
			#{content},
			#{createDt}
		)
	</insert>
	
	<update id="update">
		UPDATE hmrt_group_chat SET 	
			groupid = #{groupid},
			userid = #{userid},
			articleid = #{articleid},
			content = #{content},
			create_dt = #{createDt}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM hmrt_group_chat
		WHERE id = #{id}
	</update>
	
</mapper>